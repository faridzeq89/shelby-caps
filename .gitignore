# Flutter / Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
*.iml
.idea/

# Env / secretos (nunca en el repo)
.env
.env.*
!.env.example

# Drift generado (se regenera con build_runner)
*.g.dart

# Archivos HTML basura de un intento fallido de descarga de fuente (no borrables
# por el sandbox). La fuente real usada es assets/fonts/Nunito.ttf (sí versionada).
assets/fonts/Nunito-*.ttf

# Android
android/.gradle/
android/local.properties
android/key.properties
*.keystore
*.jks

# SO
.DS_Store
Thumbs.db
